# 来一口大转盘 · 开发手册（可复现规格 + 微信小程序移植版）

> 本文档目标：任何人照此文档都能**从零复刻**出与本程序（Web 单文件版）**功能、数据、交互、视觉完全一致**的应用，并给出**移植为微信小程序的完整规格与注意点**。
> 当前基线版本：题库 646 题；Web 版依赖 `lucky-canvas` CDN；糖果童趣 UI。
> 适用阶段：先按「Web 版复刻」对齐功能，再按「第八节」平移到微信小程序。

---

## 0. 文档目的与范围

- ✅ 覆盖：产品定义、功能清单、数据模型、核心算法、UI 视觉规格、技术架构、复现步骤、小程序移植映射。
- ❌ 不覆盖：具体 646 道题的文本内容（那属于 `question-bank.json` 数据资产，照现有结构生成/导入即可）；美术原画。
- 关键约定：**题目文字内容 = 数据，不写死在代码里**；所有类别/话题/尺度/量级**均由数据 + 配置驱动**，UI 动态渲染。

---

## 1. 产品概述

- **一句话**：一个开箱即用的随机转盘聚会小游戏，转盘一转，今晚的玩法交给运气——真心话 / 大冒险 / 直接喝 / Deeptalk 四选一，外加精准暗箱与可定制题库。
- **目标场景**：朋友聚会、桌游夜、团建、生日局等线下饮酒/破冰场景。
- **核心循环**：
  1. 玩家配置要玩的内容（开关类别、话题、尺度、量级、限制级）。
  2. 点「转一下」→ 转盘旋转 → 停下 → 展示抽中的题/任务。
  3. （可选）开启「不重复抽取」避免同一题反复出现；暗箱可指定某轮必中某题/某类。
- **合规底线**：所有参与者**知情且自愿**；限制级（spicy）内容默认关闭，需手动开启。

---

## 2. 功能清单（Feature Spec）

### 2.1 四大玩法（类别）
| 类别 id | 中文名 | 转盘色 | 权重 | 说明 |
|---|---|---|---|---|
| `truth` | 真心话 | `#ffb3c6` | 3 | 按话题分支，问问题 |
| `dare` | 大冒险 | `#ffd56b` | 3 | 带肢体接触/对象/场景多维标签 |
| `drink` | 直接喝 | `#9fd3ff` | 2 | 喝多少、谁喝、是否连锁 |
| `deeptalk` | Deeptalk | `#c9b6ff` | 1 | 深度聊天话题，按话题分支 |

### 2.2 两种转盘模式（`wheelMode`）
- **按类型（分支）** `branch`（默认）：转盘每格 = 一个**类别**（4 格）。停下后在命中的类别里随机抽一道题。
- **按题目** `question`：按类别比例从「当前生效题」里随机抽 `qSampleN`（默认 **50**）道，每道题占一格放进转盘，停下直接显示原题。
- 模式切换实时重建转盘；默认抽题数可在 UI 调整（1–1000）。

### 2.3 精准暗箱（`resolveRigging`）
可设定「第 N 次 / 前 K 次」必中**某个类别**或**某一道具体题目**：
- 规则类型：`exact`（第 N 次必中类别）、`prefix`（前 K 次必中类别）、`exactQ`（第 N 次必中某题）、`prefixQ`（前 K 次必中某题）。
- `exact` / `exactQ` 只生效一次（命中后标记 `usedExact`，不再触发）；`prefix` / `prefixQ` 在其范围内每次生效。
- 暗箱指定的题**强制进入转盘样本**（即使被过滤/不重复移除也保护），保证能按设定命中。
- 轮次（`spinCount`）归零后 `usedExact` 清空，规则可再次触发。

### 2.4 不重复抽取（`allowRepeat`）
- 默认 `false`（不重复）。抽中的题加入 `drawnIds`，从转盘移除，下次不会中（暗箱保护的题除外）。
- 不重复模式下从转盘移除时**只移除刚抽中的那一道**，其余扇区顺序保持稳定（不打乱整盘）。
- 提供「重置已抽」按钮清空 `drawnIds` 重新开玩；全部抽完提示重置。
- 允许重复（`true`）时正常随机，可能重复命中。

### 2.5 风险门控 / 限制级模式（`adultMode`）
- 每题带 `risk: safe | mild | spicy`。`spicy` 或 `adultOnly` 的题**必须开启限制级模式**才进转盘。
- 限制级模式开启时：自动勾选 spicy 话题（前任 / 迷恋暧昧 / 身体探索），并放开大冒险 `contact = 3`（全场尖叫）。关闭则收回。
- 限制级话题（当前 3 个，均 `defaultOn:false`）：`ex` 前任纠葛、`intimacy` 迷恋暧昧、`bodyexp` 身体探索。

### 2.6 大冒险四级尺度（`contact` 0–3）
| 等级 | 名字 | 示例 | 进转盘条件 |
|---|---|---|---|
| 0 | 社牛solo | 表情包模仿 / 霸总台词 | 默认开 |
| 1 | 礼貌试探 | 握手 / 击掌 | 默认开 |
| 2 | 升温互动 | 拥抱 / 喂食 | 默认开（**最大接触程度默认 2**）|
| 3 | 全场尖叫 | 贴面 / 耳语 | **需开启限制级模式** |

- 全局设置「最大肢体接触程度」`maxContact`（默认 **2**）：高于此值的题被过滤。
- 全局设置「允许骚扰陌生人」`allowStranger`（默认 **开启**）：`stranger:true` 的题受此开关控制。

### 2.7 直接喝量级（`amount`）
- 量级强度表 `AMOUNT_LEVEL = { sip:0, 'half-glass':0.5, glass:1, chug:2, bottle:3 }`。
- 全局设置「最大饮酒量级」`maxAmount`（默认 **glass**）：高于此值的题被过滤。
- 全局设置「允许指定他人喝」`allowOthers`（默认 **false**）：`who !== 'self'` 的题受此开关控制。
- `who` 取值：`self / left / right / all / choose`；`combo` 可附加「喝完再答一道题」等。

### 2.8 设置抽屉（右上角 ⚙️）
- 固定右上角的齿轮按钮，点击从右侧滑出抽屉（带半透明遮罩）。
- 抽屉内集合两项后台功能：
  - **暗箱操作**（见 2.3）
  - **题库编辑 / 导入导出**（见 2.9）
  - 以及「↻ 立即按当前设置重建转盘」按钮。
- 关闭方式：✕ / 点遮罩 / `Esc`。
- 主面板只保留 5 张玩家卡：① 转盘抽取 / ② 内容开关 / ③ 真心话话题 / ④ 大冒险·尺度 / ⑤ 直接喝·量级。

### 2.9 题库可视化编辑 / 导入导出
- 编辑器按 `cat + topic + contact尺度 + 搜索` 过滤，表格增删改查，每条显示 `risk / 尺度`，可勾选 `enabled` 启停。
- **导出 JSON**：无损备份（含 `topics` 注册表），可直接改完再导入。
- **导出 CSV**：方便用 Excel/表格批量改文字（15 列超集：id,cat,topic,text,risk,difficulty,enabled,contact,stranger,target,scene,who,amount,combo,adultOnly），带 BOM + 引号转义；CSV 导入会回退 `topic` 标签为 id（JSON 才是无损）。
- **导入整体覆盖**：导入的题库 `QB = data` **整体替换**原题库（非合并）。导入后清理失效暗箱规则引用 + 重置轮次。

---

## 3. 数据模型（题库 Schema）

### 3.1 题库根结构
```jsonc
{
  "version": 3,
  "stats": { /* 见 3.5 */ },
  "topics": {
    "truth":    [ TopicDef, ... ],
    "deeptalk": [ TopicDef, ... ]
    // dare / drink 无话题注册表，用专属字段（contact / amount）
  },
  "questions": [ Question, ... ]   // 扁平数组
}
```

### 3.2 题目类型（4 类字段表）
公共字段：`id`(字符串,带前缀,唯一) / `cat` / `text` / `risk?`(默认safe) / `difficulty?`(1–3,默认1) / `enabled?`(默认true) / `adultOnly?`(bool)。

| 类别 | 必填专属字段 | 说明 |
|---|---|---|
| `truth` | `topic`(话题id) | 真心话必挂话题 |
| `dare` | `contact`(0–3) / `stranger`(bool) / `target` / `scene?` | 多维标签，真过滤 |
| `drink` | `amount` / `who` / `combo?` | 喝法结构化 |
| `deeptalk` | `topic`(话题id) | 深度聊必挂话题 |

> 注意：`truth` / `deeptalk` 的 `topic` 必须存在于 `topics[cat]` 注册表；`dare` / `drink` **没有 `topic` 字段**（设计如此）。

### 3.3 枚举值全集
- `CategoryId` = `truth | dare | drink | deeptalk`
- `Risk` = `safe | mild | spicy`
- `Contact` = `0 | 1 | 2 | 3`
- `Target` = `self | partner | group | stranger | choose`
- `DrinkAmt` = `sip | half-glass | glass | chug | bottle`
- `Who` = `self | left | right | all | choose`
- `Scene` = `any | phone | public`

### 3.4 话题注册表（当前真实清单）
`truth`（17 个，仅前任/迷恋暧昧/身体探索 默认关且限制级）：

| id | label | 默认开 | spicy | adultOnly |
|---|---|---|---|---|
| hobby | 爱好兴趣 | ✓ | | |
| childhood | 童年回忆 | ✓ | | |
| school | 校园时代 | ✓ | | |
| family | 家庭亲情 | ✓ | | |
| friendship | 友情底线 | ✓ | | |
| love | 爱情观念 | ✓ | | |
| ex | 前任纠葛 | ✗ | ✓ | ✓ |
| secret | 黑料糗事 | ✓ | | |
| money | 金钱消费 | ✓ | | |
| work | 职场学业 | ✓ | | |
| body | 外貌身材 | ✓ | | |
| intimacy | 迷恋暧昧 | ✗ | ✓ | ✓ |
| bodyexp | 身体探索 | ✗ | ✓ | ✓ |
| role | 角色戏精 | ✓ | | |
| dream | 理想人生 | ✓ | | |
| social | 人情世故 | ✓ | | |
| weakness | 软肋恐惧 | ✓ | | |

`deeptalk`（7 个，全部默认开）：`life` 人生意义 / `fear` 内心恐惧 / `relation` 人际关系 / `self` 自我认知 / `regret` 遗憾后悔 / `value` 价值观 / `future` 未来憧憬。

### 3.5 顶部 `stats` 字段（构建时生成，可选但建议保留）
```jsonc
"stats": {
  "total": 646,
  "byCat":   { "truth":319, "deeptalk":100, "dare":199, "drink":28 },
  "byRisk":  { "safe":545, "spicy":76, "mild":25 },
  "enabled": 566, "disabled": 80,
  "generatedAt": "2026-08-03"
}
```
> 前端「题库 N 题」只显示 `stats.total`（或 `questions.length`）。

### 3.6 单条样例
```json
{ "id":"truth-ex-0003", "cat":"truth", "topic":"ex", "text":"你还留着前任送的东西吗？", "risk":"mild", "difficulty":2 }
{ "id":"dare-contact2-0012", "cat":"dare", "text":"和右边的人十指相扣 10 秒", "contact":2, "stranger":false, "target":"partner", "scene":"any" }
{ "id":"drink-chug-0007", "cat":"drink", "text":"向全场敬酒并干杯", "amount":"chug", "who":"self" }
{ "id":"deeptalk-fear-0009", "cat":"deeptalk", "topic":"fear", "text":"你最害怕失去的人是谁？为什么？" }
```

---

## 4. 当前题库统计（基线）
- **总量 646**：真心话 319 / 大冒险 199 / 直接喝 28 / Deeptalk 100。
- **风险**：safe 545 / spicy 76 / mild 25。
- **启停**：enabled 566 / disabled 80。
- 导入导出用 `question-bank.json`（含 `stats` + `topics` 注册表）。

---

## 5. 核心逻辑规格（可复现算法）

### 5.1 过滤 `isActive(q)` —— 决定一道题是否「当前进转盘」
```
if (!settings.enabled[q.cat]) return false
if (q.enabled === false) return false
if ((q.adultOnly || q.risk==='spicy') && !settings.adultMode) return false
if (q.cat in {truth, deeptalk} && settings[cat+'Topics'][q.topic]===false) return false
if (q.cat==='dare') {
  if (q.contact > settings.maxContact) return false
  if (q.stranger && !settings.allowStranger) return false
}
if (q.cat==='drink') {
  if (AMOUNT_LEVEL[q.amount] > AMOUNT_LEVEL[settings.maxAmount]) return false
  if (q.who && q.who!=='self' && !settings.allowOthers) return false
}
return true
```

### 5.2 分支构建 `buildBranches`（按类型模式）
- 对每个 `enabled` 且至少有一道 `isActive` 题的类别，生成一个 branch：`{ key, label, cat, count, questions:[活跃题] }`。
- 转盘扇区 = 这些 branch（每类别一格），扇区权重 `branchWeight = CAT_META[cat].weight * max(1, branch.count)`。
- 停下后：`pickBranch(forcedCat?)` 按权重随机选 branch → 在该 branch 的 `questions` 里均匀随机抽一道。

### 5.3 题目抽样 `buildQuestionSegs`（按题目模式）
- 从 `isActive` 题里随机抽 `qSampleN`（默认 50）道，**按各类别在活跃题中的占比分配名额**（余数轮转补齐）。
- 暗箱 `exactQ` / `prefixQ` 指向的题**强制入样**（保证能停到）。
- 每道抽样题 = 一个扇区；`qidToIndex` 记录 id→扇区下标。停下直接展示该题原文。
- 旋转前若命中的指定题未进样本，兜底回退到该类别/全体样本随机。

### 5.4 暗箱命中 `resolveRigging(n)`（n = 当前轮次 spinCount）
```
for r in rules if r.type==='prefixQ' && n<=r.count: return {kind:'q', qid:r.qid}
for r in rules if r.type==='prefix'  && n<=r.count: return {kind:'cat', cat:r.cat}
for r in rules if (r.type in {exactQ,exact}) && r.spin===n && !usedExact.has(r.id):
    usedExact.add(r.id)
    return r.type==='exactQ' ? {kind:'q',qid} : {kind:'cat',cat}
return null
```

### 5.5 旋转与结果展示时序（关键体验）
1. 点「转一下」→ `isSpinning=true`，`spinCount++`。
2. 先 `resolveRigging(spinCount)` 决定强制结果；否则按模式正常抽样/选支。
3. 结果区先显示「🎡 旋转中…」，**绝不提前显示题**（修复过的体验 bug）。
4. `myLucky.play()` 起转，`setTimeout(2600~3500ms)` 后 `myLucky.stop(idx)`。
5. **真正的结果在 `end` 回调里展示**（`stop(idx)` 动画结束才 `showResult`）。
6. 若 `!allowRepeat` 且题未被暗箱保护 → `removeDrawnFromWheel(id)` 仅移除这一道，转盘其余稳定。

### 5.6 初始默认设置（务必一致）
```
enabled: {truth:true, dare:true, drink:true, deeptalk:true}
allowStranger: true      // 默认允许骚扰陌生人
maxContact: 2            // 最大接触默认 2（升温互动）
adultMode: false
allowOthers: false
maxAmount: 'glass'       // 最大饮酒量级默认 glass
truthTopics / deeptalkTopics: 由 topics 注册表 defaultOn 初始化（限制级话题默认 false）
```

---

## 6. UI/UX 设计规格

### 6.1 设计语言
**糖果童趣风**：明亮柔和的粉/黄/浅蓝/淡紫配色，圆润图标与卡片，弹性小动效，贴合派对破冰氛围；目标群体偏年轻、轻松向。

### 6.2 配色 Token
| Token | 值 | 用途 |
|---|---|---|
| 背景渐变 | `#fff2f7 → #f3e9ff → #e8f4ff` | 页面背景（三色柔和渐变）|
| 主粉 | `#ff9ec4` / `#ff6b9d` | 强调、转盘中心钮、按钮渐变 |
| 黄（高亮/设置钮）| `#ffd56b` | 主按钮、齿轮 |
| 卡片底 | 白色 + 2px 粉描边 + 粉色柔影 | 卡片 |
| 类别色 | truth `#ffb3c6` / dare `#ffd56b` / drink `#9fd3ff` / deeptalk `#c9b6ff` | 转盘扇区、标签 pill |
| 分隔线 | 粉色虚线 | 卡片内分隔 |
| 文字 | 深梅 `#5a3a47` 等深色调 | 正文 |

### 6.3 字体
- 中文可爱体：**ZCOOL KuaiLe**（站酷快乐体），用于标题。
- 拉丁/数字：**Nunito**，用于数字、英文。
- 来源：Google Fonts（`<link>` + `preconnect`）。离线需改为本地字体。

### 6.4 布局与组件
- 移动优先、居中单栏；卡片圆角 18–24px；hover/点击用弹性曲线 `cubic-bezier(.34,1.56,.64,1)` 做 `translateY + rotate + scale` 弹跳。
- 转盘区固定居中（不浮动）；深色/浅色主题都需保证对比度。
- 结果区 pill 用类别色背景 + 白字。

### 6.5 设置抽屉交互
- 右上角固定 `⚙️` 圆钮（黄粉渐变，hover 旋转）→ 右侧滑出抽屉（`translateX` + 弹性曲线）+ 半透明遮罩。
- 关闭：✕ / 点遮罩 / `Esc`。

---

## 7. 技术栈与架构（当前 Web 版）

### 7.1 依赖
- **lucky-canvas** v1.7.25（转盘渲染，CDN 引入 `https://unpkg.com/lucky-canvas` 或 jsdelivr）。
- Google Fonts：ZCOOL KuaiLe + Nunito（CDN）。
- 纯原生 HTML/CSS/JS，无其他框架。

### 7.2 文件结构
| 文件 | 说明 |
|---|---|
| `app.html` | **主程序**：自包含单文件，双击即玩（题库已内联，仅依赖 CDN）|
| `app.src.html` | 源码模板（含题库占位符 `__QUESTION_BANK__`），由 build.js 注入后产出 app.html |
| `build.js` | 构建脚本：读 `question-bank.json` → 注入 `app.src.html` → 写 `app.html` |
| `question-bank.json` | 题库数据（含 stats + topics 注册表），可外部编辑后导入 |
| `gen-bank.js` | 题库生成器：模板+词池批量生成（含去重），可选重生成 |
| `question-bank-design.md` | 题库分类与字段设计文档 |
| `index.html` | 轻量旧版 starter（仅供参考，非主程序）|
| `README.md` | 使用说明 |

### 7.3 构建 / 运行
```bash
# 开发：改 app.src.html 或 question-bank.json 后
node build.js          # 生成自包含 app.html
# 重做整套题库（可选）
node gen-bank.js
# 直接玩：双击 app.html（或本地起静态服务器）
```

---

## 8. 微信小程序移植指南（目标）

> 小程序版目标是**功能 1:1 复刻 + 视觉风格一致**，数据用小程序本地存储替代「文件导入导出」。

### 8.1 技术选型
- **转盘**：使用 `lucky-canvas` 的**微信小程序适配版**（npm 包 `@lucky-canvas/wx-vue` 或官方小程序组件），勿用 Web 的 CDN 版。
- **框架**：原生小程序（WXML/WXSS/JS）或 Taro/uni-app 均可；本手册按原生描述。
- **状态管理**：页面 `data` + `setData`；复杂状态可放 `app.globalData` 或 `getApp()`。
- **存储**：`wx.getStorageSync / setStorageSync` 替代文件 IO（题库默认内置为 JS/JSON 模块，用户导入的题库存 storage）。

### 8.2 目录结构映射（Web → 小程序）
```
Web                      →  小程序
app.html/app.src.html    →  pages/index/index.wxml + .wxss + .js
build.js 注入            →  无需构建：题库直接 import 为模块或放 storage
question-bank.json       →  /data/bank.js (export default) 或 云端配置
gen-bank.js              →  离线脚本（同，Node 运行，不进小程序包）
README.md                →  项目说明
```

### 8.3 数据层重构
- 内置题库：`/data/bank.js` 导出 `QuestionBank` 对象（等同于 `window.QUESTION_BANK`）。
- 「导入导出」改为：导出 → `wx.getFileSystemManager().writeFile` 存到用户目录 / 或复制文本到剪贴板；导入 → 从聊天/文件选择 JSON → `JSON.parse` → `setStorageSync('bank', data)`。
- 编辑器改动写回 `setStorageSync('bank', QB)`；启动时 `QB = getStorageSync('bank') || 内置题库`。

### 8.4 各功能模块映射表
| Web 实现 | 小程序实现要点 |
|---|---|
| `lucky-canvas` CDN | 小程序版 `lucky-canvas` 组件，属性/回调同名（`play/stop/end`）|
| `CAT_META` 常量 | 放 `index.js` 顶部或独立 `config.js` |
| `isActive / resolveRigging / buildBranches / buildQuestionSegs` | 纯 JS 逻辑，**原样搬**（`app.js` 或 `utils/`），不依赖 DOM |
| 设置抽屉 `⚙️` | `wx.showModal` 太重 → 用自定义 `cover-view` 抽屉组件或 `popup` 组件 |
| 题库编辑表格 | `scroll-view` + `wx:for` 渲染，编辑走 `setData` |
| 不重复抽取 `drawnIds` | 页面 `data.drawnIds`（数组），`setData` 更新 |
| 字体 ZCOOL KuaiLe | 小程序不支持 Google Fonts 直引 → 用 `wx.loadFontFace` 加载本地/网络字体，或降级为系统圆体 |

### 8.5 小程序特有注意点（务必提前规划）
1. **审核合规（最重要）**：
   - 微信对「引导饮酒 / 赌博 / 低俗 / 性暗示」审核严格。**限制级（spicy）内容默认必须关闭**，并在设置页显著标注「仅限成年人、知情自愿」。
   - 避免任何露骨成人文本；`intimacy/bodyexp` 类保持「暧昧/好奇」尺度，不越界。
   - 建议加「本程序仅供娱乐，请理性饮酒、未成年人禁用」免责声明页。
2. **字体**：`ZCOOL KuaiLe` 需通过 `wx.loadFontFace` 加载（网络字体需配置 downloadFile 合法域名），否则用系统字体兜底。
3. **包体积**：内置题库 646 题 JSON 约 150KB，放主包无压力；若扩到 1000+ 题，题库可放**分包**或**云存储**按需拉取。
4. **转盘性能**：小程序 `setData` 频繁更新大数组会卡；转盘扇区数据只在「重建」时 `setData` 一次，旋转期间不要高频更新。
5. **分享**：可加 `onShareAppMessage` 让玩家分享「今晚抽到的题」截图（`wx.canvasToTempFilePath`）。
6. **暗箱/不重复的轮次状态**：小程序页面 `onUnload` 会丢失内存状态，需把 `spinCount / drawnIds / rigging` 持久化到 storage（或放 `globalData` 且页面栈不销毁时保留）。

### 8.6 复刻 Checklist（小程序）
- [ ] 初始化项目，引入小程序版 `lucky-canvas`。
- [ ] 搬运 `CAT_META`、`AMOUNT_LEVEL`、四类枚举、`settings` 默认值。
- [ ] 搬运 `isActive / buildBranches / buildQuestionSegs / resolveRigging / pickQuestion / pickBranch` 纯逻辑（不改语义）。
- [ ] 内置题库模块 = 当前 `question-bank.json`（含 stats + topics）。
- [ ] 实现两种转盘模式 + 不重复抽取 + 精准暗箱 + 重置。
- [ ] 实现设置抽屉：内容开关、话题开关、大冒险尺度、直接喝量级、限制级模式。
- [ ] 实现题库编辑器 + 导入导出（storage 版）。
- [ ] 套用糖果童趣视觉（配色 Token + 弹性动效 + 字体处理）。
- [ ] 加免责声明 + 限制级默认关，过审准备。
- [ ] 真机调试转盘性能与旋转时序（确保 `end` 回调才出结果）。

---

## 9. 复现步骤总览（从零到能跑）

**Web 版（对齐当前产物）：**
1. 准备 `question-bank.json`（结构见第 3 节，基线 646 题）。
2. 准备 `app.src.html`（HTML 骨架 + 样式 + 全部 JS 逻辑，题库处留 `__QUESTION_BANK__` 占位）。
3. `node build.js` 注入题库 → 生成 `app.html`。
4. 双击 `app.html` 验证：四类玩法、两种模式、暗箱、不重复、设置抽屉、编辑器、导入导出全部正常。

**小程序版（平移）：**
1. 按 8.2 建目录，引入小程序版 `lucky-canvas`。
2. 按 8.3 把题库变内置模块 + storage 导入导出。
3. 按 8.4 / 8.6 搬运纯逻辑 + 实现 UI + 视觉。
4. 过审准备（8.5）后预览/上传。

---

## 附：当前仓库与文件
- GitHub（公开）：`https://github.com/MistStride/drinking-wheel-game`
- 主程序：`app.html`（自包含，双击即玩）
- 源码：`app.src.html` / `build.js` / `gen-bank.js` / `question-bank.json` / `question-bank-design.md` / `README.md`
