# 🍻 来一口大转盘 · 用户手册

一份给玩家的图文使用说明。从双击打开，到暗箱、自定义比例、改题库，一步步讲清楚。

> ⚠️ 使用前请确保所有参与者知情且自愿参与，并自觉把握玩笑尺度。本工具仅供学习与非商业聚会娱乐。

---

## 一、怎么打开

1. 找到仓库里的 `app.html` 文件，**双击**用浏览器打开即可（Chrome / Edge / Safari 都行）。
2. 无需安装、无需联网、无需服务器 —— 题库 646 题已经内嵌在文件里，所有抽题、暗箱、去重都在你自己的电脑上完成。
3. 想自己改题目？看 [自定义题库指南](自定义题库指南.md)，复制一段 prompt 发给 AI 就能生成专属题库。

打开后你会看到：中间一个大转盘、底部一个「开始」按钮、右上角一个 **⚙️ 设置** 图标。

---

## 二、基本玩法：转一次

1. 点中间转盘下方的 **开始**（或直接在转盘上点一下），转盘开始旋转。
2. 转盘停下，指针指向哪一格，就执行那一格对应的内容：
   - **按类型模式**：每一格是一个类别（如「真心话」），停下后再在该类里随机抽一道具体题。
   - **按题目模式（默认）**：每一格就是一道具体的题，停下直接显示原题。
3. 按屏幕上的提示执行即可。

---

## 三、玩法设置（转盘上方的开关）

在转盘上方一排设置里，你可以决定这场游戏「玩什么、玩多猛」：

### 1. 四大玩法开关
默认四个都开。不想玩某类，把对应开关关掉即可（例如只想玩真心话，就把大冒险 / 直接喝 / Deeptalk 关掉）。

- **真心话** 💬
- **大冒险** 🔥
- **直接喝** 🍺
- **Deeptalk（深度聊）** 🌊 —— 偏走心、聊价值观的开放式问题

### 2. 高能模式（限制级话题）
- 默认**关闭**。
- 开启后解锁「前任 / 暧昧 / 身体探索」等 spicy 话题，并且放开大冒险最猛的 **3 级·全场尖叫**（贴面 / 耳语）。
- 建议只对彼此很熟的局开启，并事先征得大家同意。

### 3. 真心话 / Deeptalk 话题范围
- 点开可勾选具体话题方向（如「童年」「恋爱观」「社死现场」）。
- 只想聊某些轻松话题，就只勾那几个；不勾的不会进入转盘。

### 4. 大冒险尺度
- 大冒险按「身体接触程度」分 4 级：
  - `0 社牛solo`（表情包模仿 / 霸总台词）
  - `1 礼貌试探`（握手 / 击掌）
  - `2 升温互动`（拥抱 / 喂食）
  - `3 全场尖叫`（贴面 / 耳语，需开启高能模式）
- 可设一个**最大尺度等级**，超过的不会出现。

### 5. 直接喝 · 量级
直接喝类题目按「喝多少」分 3 档：
- `最温和 · 趣味喝法`（sip）
- `喝一杯`（glass）
- `罚酒干杯 · 最猛`（chug）

另外还有「指定谁喝」：
- **自己喝**：乖乖自己喝。
- **指定其他人喝 / 接力**：转盘停下后由当前玩家指定一个人，或轮流接力（由「允许指定他人喝 / 接力」总开关控制）。

---

## 四、转盘抽取模式（⚙️ 设置里）

打开右上角 **⚙️**，第一项就是「转盘抽取」，有两种模式：

| 模式 | 说明 | 适合场景 |
| --- | --- | --- |
| **按题目（默认）** | 从所有生效题里按比例随机抽 N 道（默认 50）放进转盘，每格就是一道题 | 想让转盘里有具体题目、直接看题执行 |
| **按类型（分支）** | 每格是一个类别，停下后再在类别内随机抽一道 | 想保留「先定大类再抽题」的悬念感 |

切换即时生效，转盘会自动重建。

---

## 五、自定义比例（按题目模式专属）

这是「按题目」模式下的进阶玩法：**精确控制每类、甚至每个二级分支放进转盘的题目数量**。

1. 确认当前是「按题目」模式（默认就是）。
2. 点「⚖️ 自定义比例」按钮，弹出设置窗。
3. 窗口里列出所有二级分支（如真心话·童年、大冒险·升温互动、直接喝·喝一杯……），每个分支有一个滑块：
   - 滑块**上限** = 该分支题库里当前生效的题目总数（绝不会超过，不会抽空题库）。
   - 拖动滑块设定这个分支放几道进转盘；留 0 表示不放该类。
4. 底部按钮：
   - **等比铺满**：按当前总题数（N）自动把各分支按占比铺满。
   - **全体清零**：把所有自定义数量清空，回到默认比例。
   - **保存并应用**：确认设置，转盘按新比例重建。
5. 顶部会实时显示「已选总数」，方便你控制转盘大小。

> 小提示：分支上限会随你开关「高能模式 / 话题范围 / 大冒险尺度」而实时变化，不用手动算。

---

## 六、不重复抽取

- 默认开启「允许重复抽取」。
- 关掉后，抽中过的题会从转盘移除，下一轮不会再中（除非它被暗箱保护）。
- 想重新来过，点「**重置已抽**」即可把所有题放回转盘。

---

## 七、暗箱操作（⚙️ 设置里）

想「作弊」让某人必中某类、或某道特定题一定出现？用暗箱。

1. 打开右上角 **⚙️**，进入「暗箱操作」。
2. 支持四类规则：
   - **指定第 N 次必中某类**：例如「第 3 次一定转到真心话」。
   - **前 K 次必中某类**：例如「前 2 次都先出大冒险」。
   - **指定某道具体题必中**：先在题库编辑里搜到那道题，复制它的题号，粘贴进来设定「第 N 次 / 前 K 次必中这道题」。
   - （具体题必中时，系统会强制把它放进转盘，保证能停到它。）
3. 规则可叠加、可删除，设定后即时生效。

> 暗箱适合「整活」：比如朋友生日那次，提前埋一道「对寿星说三句真心话」的题必中。

---

## 八、题库编辑（⚙️ 设置里）

在「⚙️ → 题库编辑」里，你可以查看、搜索、新增、导入导出题目。

### 1. 一条一条加（最推荐，小白友好）
- 在「＋ 新增题目」面板：填题干 → 选分类 → 选二级（话题范围 / 尺度 / 量级）→ 点「＋ 新增」。
- 真心话 / Deeptalk 还能在话题范围旁点「＋ 新增分类」，自己起一个二级话题名。
- 直接喝可同时选「量级」和「指定谁喝」。

### 2. 搜索 / 筛选
- 顶部搜索框按关键词找题。
- 按分类 + 二级（话题范围 / 尺度 / 量级）联动筛选。

### 3. 导入导出
- **导出 JSON**：无损备份，题目所有字段都在。
- **导出 CSV**：方便用 Excel / 表格改文字。
- **导入 JSON / CSV**：改完再导回覆盖。
- 改完点「重建」让转盘生效。

### 4. 自定义题库指南
- 点「📖 自定义题库指南」弹出说明，里面教你**复制一段 prompt 发给 AI**，让 AI 帮你生成一整套专属题目，再导入使用。
- 弹窗里也有「📥 下载交给 AI 的题库指南（.md）」按钮，把说明文件下载下来转发给 AI。

---

## 九、常见问题（FAQ）

**Q：打开是空白 / 转盘不转？**
A：用现代浏览器（Chrome / Edge / Safari）打开；个别老旧浏览器对本地单文件限制较严。确认文件是 `app.html`（不是 `app.src.html`）。

**Q：为什么有些大冒险题（全场尖叫）一直不出现？**
A：那是 3 级接触类，需要先开启「高能模式」才会进题库。

**Q：自定义比例里某个分支上限很小？**
A：上限 = 当前生效题数。关掉高能模式、收 narrower 话题范围、或调低大冒险最大尺度，都会让对应分支上限变少。

**Q：暗箱设了必中某题，但转盘格数不够？**
A：系统会强制把该题为优先放进转盘，并保证能停到它；其余题目按剩余比例分配。

**Q：改完题库想恢复出厂？**
A：重新双击原始 `app.html`（或重新 `node build.js` 生成）即可回到内置 646 题。

---

## 十、一句话总结

打开 `app.html` → 用顶部开关选玩法（默认按题目抽 50 道）→ 想精细控量就点「⚖️ 自定义比例」→ 想整活就进 ⚙️ 设暗箱 → 想换题就进 ⚙️ 题库编辑。玩得开心，也玩得得体 🍻
