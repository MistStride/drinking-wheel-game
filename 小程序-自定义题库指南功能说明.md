# 来一口大转盘 ·「自定义题库指南」功能交接说明（给小程序）

> 目标：在小程序里复刻网页版已有的「自定义题库指南」功能——让用户知道题库能自定义、有几种玩法、并拿到一份「发给 AI 生成题库」的说明文档。
> 适用：微信小程序（WXML / WXSS / JS），UI 风格与网页版「糖果童趣」保持一致。

---

## 一、功能逻辑（用户视角）

```
用户在「题库编辑」页
   └─ 点 [📖 自定义题库指南] 按钮
        └─ 弹出说明弹窗（cover-view / 自定义 modal）
             ├─ 标题：📖 自定义你的题库
             ├─ 引言：不想用官方 646 题？三种换题方式
             ├─ 玩法①：界面一条条加（最推荐，零基础）
             ├─ 玩法②：复制一段话给 AI 生成全套
             ├─ 玩法③：在官方题库上加菜
             ├─ 导入提醒：拿到 .json → 导入 JSON（会整体覆盖，先备份）
             └─ 底部按钮 [📥 下载交给AI的题库指南（.md）]
                  └─ 把完整指南文本交给用户（见下方「小程序适配」）
```

**核心要点**
- 弹窗只做「说明 + 交付文档」，不做编辑逻辑（编辑逻辑是题库编辑器自己的事）。
- 按钮名是「自定义题库指南」（点击=开弹窗），不是直接下载。
- 下载按钮放在弹窗内、说明下方，文案：`📥 下载交给AI的题库指南（.md）`。
- 弹窗层级必须高于设置抽屉（网页版 z-index: 90 > 抽屉 70）。

---

## 二、要给小程序的文件清单

| # | 文件 | 路径 | 给小程序什么 |
|---|------|------|--------------|
| 1 | **自定义题库指南.md** | `drinking-wheel/自定义题库指南.md` | 弹窗里要展示/交付的完整说明正文（即「交给 AI 的题库指南」本体）。小程序可整篇展示，或只取其中第三/四节的「AI 需求模板」 |
| 2 | **我的题库模板.json** | `drinking-wheel/我的题库模板.json` | 最小可运行题库范本（6 题覆盖四类），用于「附录」里让用户下载改文字、或整份贴给 AI 当格式范本 |
| 3 | **question-bank.json** | `drinking-wheel/question-bank.json` | 官方题库真实结构（646 题），小程序题库编辑器复用同一 schema；也是「玩法③ 加菜」的源数据 |
| 4 | **本说明文档** | `drinking-wheel/小程序-自定义题库指南功能说明.md` | 即本文件，描述逻辑、UI 规范、适配点 |
| 5 | **弹窗实现片段**（参考） | `app.src.html` 第 318–354 行（HTML）、201–217 行（CSS）、1154 行（JS 绑定） | 网页版弹窗的 HTML 结构 / 样式 / 开关逻辑，照着翻译成 WXML/WXSS/JS |

> 最简交付：把 **文件 1（md 正文）** 内联进小程序页面即可实现核心功能；文件 2/3 作为题库编辑器的数据依赖本就存在，文件 5 是 UI 参考。

---

## 三、弹窗 UI 规范（网页版 → 小程序映射）

### 3.1 结构（网页版 HTML，来自 app.src.html:321–354）
```
guide-overlay          ← 全屏遮罩（半透明黑）
  guide-modal          ← 居中卡片（白底、粉边、大圆角、阴影）
    guide-close-x      ← 右上角 ✕ 关闭按钮（圆形）
    h3   📖 自定义你的题库
    p    引言一句
    gstep ×3            ← 三个玩法卡片（淡粉底、粉边、圆角）
    h4   📥 怎么把自定义题库用起来
    p    导入提醒
    guide-dl           ← 底部下载按钮（黄色渐变）
```

### 3.2 视觉 token（小程序用 rpx 等价）
| 元素 | 网页(px) | 小程序(rpx) | 样式 |
|------|----------|-------------|------|
| 遮罩 | — | — | `background: rgba(70,58,65,.45)` |
| 弹窗卡片 | 圆角 26 / 白底 / `border:2px solid var(--line)` | 52rpx / #fff / `2rpx solid #ffc9de` | `box-shadow: 0 14rpx 34rpx rgba(255,142,177,.22)` |
| 关闭钮 | 32×32 圆 | 64×64 圆 | 白底、粉边、`✕` 深粉；hover 旋转 |
| 玩法卡片 | 圆角 16 / 淡粉底 | 32rpx / `#fff7fb` | `border:2rpx solid #ffc9de` |
| 下载钮 | 圆角 14 / 黄渐变 | 28rpx / `linear-gradient(135deg,#ffd56b,#ffc24d)` | 文字 `#7a4a5e`、加粗、带投影 |
| 标题字 | ZCOOL KuaiLe | 同网页（需 `@font-face` 引入 ttf，或退用系统字体加粗）| 深粉 `#ff6b9d` |

> 字体：小程序不支持 Google Fonts 外链。若要和网页一致，把 `ZCOOL KuaiLe` 转 base64 放 `static/fonts/` 用 `@font-face`；否则用 `PingFang SC` 加粗 + 大圆角补救。

### 3.3 交互
- 点遮罩空白处 / ✕ → 关闭弹窗（`bindtap` 判断 `e.target === overlay`）。
- 点「自定义题库指南」按钮 → `overlay.show = true`。
- z-index：弹窗须高于题库编辑页其它浮层。

---

## 四、小程序适配要点（和网页版的关键差异）

1. **「下载 .md」在小程序里改为「复制全文」**
   小程序无法像网页那样 `Blob` 下载 .md 再让用户拿去发 AI。最顺手的做法是：
   - 底部按钮文案改为 `📋 复制交给AI的题库指南`；
   - 点击调用 `wx.setClipboardData({ data: 指南全文 })`，提示「已复制，去微信 AI 粘贴即可」；
   - 若仍想保留「下载」，可用 `wx.getFileSystemManager().writeFile` 存到小程序本地目录，再 `wx.openDocument` 打开（体验不如复制直接，推荐用复制）。

2. **指南正文来源**
   - 把 `自定义题库指南.md` 全文作为字符串常量内联进小程序 JS（同网页版 `window.GUIDE_MD` 思路）；
   - 弹窗里用 `<text user-select>` 渲染，关键「AI 需求模板」代码块可单独给一个「复制这段」按钮。

3. **题库 schema 复用**
   小程序题库编辑器直接读 `question-bank.json` 的同一结构（`version/stats/topics/questions`，四类 `cat` + 各自字段），无需重新设计。导入校验规则与网页版一致（cat 取值、contact 0–3、enabled、高能模式解锁 spicy/contact=3）。

4. **「界面一条条加」已在小程序题库编辑器内**
   弹窗玩法①只是引导用户去用编辑器的新增功能，无需在弹窗里重复实现。

---

## 五、可直接发给小程序开发的任务 prompt

> 把下面的文字 + 文件 1/2/3/5 一起发给小程序对话即可。

```
请在「来一口大转盘」微信小程序里新增「自定义题库指南」功能，复刻网页版行为：

1. 在题库编辑页的导入导出按钮旁边，加一个 [📖 自定义题库指南] 按钮。
2. 点击弹出说明弹窗（层级高于题库编辑页），内容包含：
   - 标题「📖 自定义你的题库」+ 一句引言；
   - 三种玩法说明：① 界面一条条加（引导去用编辑器新增）② 复制一段话给 AI 生成全套 ③ 在官方题库上加菜；
   - 「怎么用起来」导入提醒（拿到 .json → 导入 JSON，会整体覆盖需先备份）；
   - 底部按钮 [📋 复制交给AI的题库指南]，点击把完整指南文本写入剪贴板（wx.setClipboardData），提示用户去微信 AI 粘贴。
3. 指南正文直接使用随附的「自定义题库指南.md」全文（内联为字符串常量）。
4. 视觉风格与小程序现有「糖果童趣」一致：白底卡片 + 粉边 + 大圆角 + 柔和阴影；关闭钮右上角圆形✕；底部按钮黄色渐变。
5. 题库 schema 复用现有的 question-bank.json 结构，不重新设计。
6. 弹窗 z-index 须高于题库编辑页其它浮层。
```

---

## 附：网页版实现片段（app.src.html 参考）

**HTML（318–354 行）**
```html
<!-- 自定义题库指南弹窗 -->
<div class="guide-overlay" id="guideOverlay">
  <div class="guide-modal">
    <button class="guide-close-x" id="guideClose" aria-label="关闭">✕</button>
    <h3>📖 自定义你的题库</h3>
    <p>不想用官方那 646 道题？三种方式给你的转盘换上专属题目，从最省事到最自由：</p>
    <div class="gstep"><b>① 在界面上一条条加（最推荐，零基础）</b>…</div>
    <div class="gstep"><b>② 复制一段话给 AI，一键生成整套题库</b>…</div>
    <div class="gstep"><b>③ 在官方题库上加菜</b>…</div>
    <h4>📥 怎么把自定义题库用起来</h4>
    <p>…点 ⬇ 导入 JSON…</p>
    <button class="guide-dl" id="guideDownload">📥 下载交给AI的题库指南（.md）</button>
  </div>
</div>
```

**JS 绑定（1154 行附近）**
```js
const guideOverlay = document.getElementById('guideOverlay');
document.getElementById('btnGuide').addEventListener('click', () => guideOverlay.classList.add('show'));
document.getElementById('guideClose').addEventListener('click', () => guideOverlay.classList.remove('show'));
guideOverlay.addEventListener('click', e => { if (e.target === guideOverlay) guideOverlay.classList.remove('show'); });
document.getElementById('guideDownload').addEventListener('click', doExportGuide);
// doExportGuide: Blob 下载 window.GUIDE_MD 为「自定义题库指南.md」
```

**CSS（201–217 行）**：`.guide-overlay{z-index:90}`、`.guide-modal` 白底粉边圆角阴影、`.guide-dl` 黄渐变、`.guide-close-x` 圆形等，详见源文件。
