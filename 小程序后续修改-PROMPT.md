# 「来一口大转盘」网页版 → 微信小程序 同步修改 PROMPT

> 用途：开发手册（commit `aa97531`）是我在说要移植小程序之前写的快照。以下是我**在说出要做小程序之后、对网页版做的所有代码改动**。请基于最新版 `app.src.html` / `app.html` 把这些改动同步到小程序，不要只照旧手册。

---

## 背景（请先读这两份作为基准）
1. 小程序开发手册（已写在 `来一口大转盘-开发手册.md`）。
2. 最新网页源码 `app.src.html`（含全部 UI 与逻辑，是 truth source）。
3. 题库 `question-bank.json`（646 题：truth 319 / dare 199 / drink 28 / deeptalk 100；enabled 566 / disabled 80）。

---

## 需同步到小程序的改动清单

### 1. 转盘扇区标签去掉「题目数量（数字）」
- **之前**：扇区显示 `类别名(数量)`，例如 `社牛solo(60)`。
- **现在**：只显示类别名，如 `社牛solo`。
- **小程序做法**：转盘扇区文字只渲染 `branchLabel`（类别名），不要再拼接 `(` + count + `)`。

### 2. 大冒险分支名去掉「大冒险 ● 」前缀
- **之前**：转盘显示 `大冒险 ● 社牛solo`。
- **现在**：直接显示 `社牛solo`。
- **小程序做法**：`branchLabel` 直接返回等级名，不要加 `大冒险 ● ` 前缀。

### 3. 大冒险「等级 → 分支名」映射修正（关键 bug，务必照此实现）
- 大冒险（dare）按 `contact` 字段映射：
  - `contact = 0` → `社牛solo`
  - `contact = 1` → `礼貌试探`
  - `contact = 2` → `升温互动`
  - `contact = 3` → `全场尖叫`
- 若 `stranger = true`（骚扰陌生人），分支名固定为 `陌生人互动`，**不**走上面 0–3 等级体系。
- 实现要点：用连续数组 `['社牛solo','礼貌试探','升温互动','全场尖叫']`，下标直接对应 `contact`（**不要**在第一位放占位空串，那会造成整体错位、contact=0 退化成「大冒险」的怪格）。
- 结果：「按类型」模式下大冒险候选共 5 类（4 个等级 + 陌生人互动），但第 5 类（全场尖叫）默认不进转盘（见第 5 条）。

### 4. 限制级模式说明文案精简
- **之前**：`（解锁前任 / 迷恋暧昧 / 身体探索 等 spicy 话题…）`
- **现在**：`限制级模式（解锁前任 等 spicy 话题，并放开大冒险「全场尖叫（3 级）」）`
- **小程序做法**：限制级开关旁文案同步为上面这句。

### 5. 第五类「全场尖叫」(contact=3) 默认关闭
- 题库中 contact=3 共 **28 道**，全部 `enabled: false` 且 `adultOnly: true`。
- 默认状态（限制级关、尺度 ≤ 2）下，第 5 类**不会**出现在转盘。
- **小程序做法**：
  - 题库里这 28 道默认 `disabled`。
  - 即便用户在设置里把尺度调到 3、并开启限制级模式，仍需这些题 `enabled` 才会显示（当前产品决策：默认**不**启用，需用户在题库编辑里手动打开）。
  - 因此默认转盘大冒险只有 4 类：`社牛solo / 礼貌试探 / 升温互动 / 陌生人互动`。

### 6. 设置抽屉（⚙️）内两块顺序对调
- **之前**：⑥ 暗箱操作 → ⑦ 题库编辑 / 导入导出。
- **现在**：⑥ 题库编辑 / 导入导出（前置）→ ⑦ 暗箱操作（末尾）。
- `↻ 立即按当前设置重建转盘` 按钮跟随「暗箱操作」，放在设置抽屉最底部。
- **小程序做法**：设置页/抽屉里的卡片顺序与编号（⑥/⑦）照此调整。

---

## 无需同步到小程序（仅网页部署相关，备忘）
- `build.js` 现同时生成 `index.html`（GitHub Pages 默认首页），修掉了 Pages 显示旧界面的问题。小程序无此构建/托管链路，忽略即可。

---

## 一句话总括（可直接粘到小程序对话开头）
「我之前给了你开发手册，但那之后网页版又改了 6 处：①转盘扇区不再显示题量(数字)；②大冒险分支名去掉『大冒险 ●』前缀；③修正了大冒险等级→分支名映射（contact 0-3 = 社牛solo/礼貌试探/升温互动/全场尖叫，陌生人单独为『陌生人互动』，数组不要留空串）；④限制级说明文案精简；⑤第五类『全场尖叫』(contact=3 共28题)默认 disabled、不进转盘；⑥设置抽屉改为『题库编辑(⑥)在前、暗箱操作(⑦)在后、重建按钮最末』。请以最新 `app.src.html` 为准，把以上同步进小程序。」
