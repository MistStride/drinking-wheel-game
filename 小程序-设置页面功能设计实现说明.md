# 来一口大转盘 · 设置页面设计与实现（小程序重构交接文档）

> 适用对象：小程序重构工程师
> 来源：网页版 `drinking-wheel/app.src.html`（自包含单文件）
> 本文把网页版"设置相关"的所有功能拆成「设计意图 / 数据模型 / 关键实现 / 小程序适配」四段，可直接照着落地。

---

## 0. 总览：设置页面由哪几块组成

网页版的设置相关 UI 分两部分：

| 区块 | 位置 | 包含功能 |
|---|---|---|
| **主控制面板**（常驻右侧 `panel`） | ①~⑤ | 转盘抽取、内容开关＋高能模式、真心话话题范围、大冒险尺度、直接喝量级 |
| **设置抽屉**（⚙️ 从右侧滑出 `settingsDrawer`） | ⑥~⑦ | 题库编辑/导入导出、暗箱操作、立即重建 |

小程序建议保留同样的两层结构：常驻的「玩法设置」用页面内分区，较重的「题库编辑/暗箱」用半屏弹层（`wx.showHalfScreenDialog` 或自定义组件）。

---

## 1. 核心数据模型（小程序务必先建好这两个状态）

### 1.1 分类元信息 `CAT_META`（写死，不存用户态）

```js
const CAT_META = {
  truth:    { label: '真心话',   color: '#ffb3c6', weight: 3 },
  dare:     { label: '大冒险',   color: '#ffd56b', weight: 3 },
  drink:    { label: '直接喝',   color: '#9fd3ff', weight: 2 },
  deeptalk: { label: 'Deeptalk', color: '#c9b6ff', weight: 1 },
};
```

- `color`：转盘扇区底色（相邻扇区用 `shade(color,-18)` 错开明暗）。
- `weight`：分支模式下该类的抽中权重（见 §2）。

### 1.2 运行设置 `settings`（用户态，需本地缓存）

```js
const settings = {
  enabled: { truth: true, dare: true, drink: true, deeptalk: true }, // 四类总开关
  truthTopics: {}, deeptalkTopics: {},   // 话题范围开关，key=topic.id，value=是否启用
  allowStranger: true, maxContact: 2, adultMode: false,  // 大冒险
  allowOthers: false, maxAmount: 'glass',                 // 直接喝
};
// 初始化时把题库里的话题默认状态灌进 settings：
QB.topics.truth.forEach(t => settings.truthTopics[t.id] = t.defaultOn !== false);
QB.topics.deeptalk.forEach(t => settings.deeptalkTopics[t.id] = t.defaultOn !== false);
```

> 小程序：`settings` 和自定义后的 `QB` 一起 `wx.setStorageSync('settings', …)` / `wx.setStorageSync('questionBank', …)` 持久化，避免每次进页面重置。

### 1.3 题库对象 `QB`

```js
QB = {
  version: 'x.y',
  questions: [ /* 见下 */ ],
  topics: {
    truth:    [{ id, label, defaultOn, adultOnly? }, …],
    deeptalk: [{ id, label, defaultOn, adultOnly? }, …],
  },
  stats: { note: '…' },
};

// 单题字段（按 cat 取用）
{
  id, cat: 'truth'|'dare'|'drink'|'deeptalk',
  text, risk: 'safe'|'mild'|'spicy', difficulty: 1,
  enabled: true,            // 是否进转盘（编辑器可关）
  adultOnly: false,         // spicy/高能话题：需 adultMode 才出现
  // 仅 truth/deeptalk：
  topic: '<topicId>',
  // 仅 dare：
  contact: 0|1|2|3,         // 肢体接触尺度
  stranger: false,          // 是否骚扰陌生人
  target: 'self', scene: 'any',
  // 仅 drink：
  amount: 'sip'|'glass'|'chug',   // 量级（sip=趣味喝法/glass=喝一杯/chug=罚酒干杯）
  who: 'self'|'other',             // 指定谁喝
  combo: false,                    // 是否带附加题
}
```

> 注意：`amount` 真实数据里还有历史值 `half-glass`/`bottle`，编辑/显示时要兜底（见 §6 表格）。

---

## 2. ① 转盘抽取（抽题方式）

**设计意图**：两种转法，满足不同玩法密度。

| 模式 | 含义 | 扇区内容 |
|---|---|---|
| `branch`（按类型/分支，默认） | 每格 = 一个类别或二级分支 | 停下后在该分支内随机抽一题展示 |
| `question`（按题目） | 先按类别比例随机抽 `qSampleN` 道 | 每格 = 一道题，停下直接显示原文 |

**配置项**
- `wheelMode`：`'branch'` | `'question'`
- `qSampleN`：按题目模式抽多少道（默认 50，按各类别在生效题中的占比分配名额）
- `allowRepeat`：是否允许重复抽中（`false` 时抽中后该题移出转盘，存 `drawnIds`）

**关键实现**
- `buildBranches()`：`QB.questions` 经 `isActive()` 过滤后，按 `cat::branchLabel(q)` 分组，每组 `count` + `questions[]`。
- `buildQuestionSegs()`：从生效题按类别占比抽样 `qSampleN` 道；**暗箱精准指定的题强制入样**（`forcedIds`），保证能停到它。
- `pickBranch(cat)`：权重 = `CAT_META[cat].weight * max(1, b.count)`，按权重随机。
- `pickQuestion(cat)`：在 `qSegs` 里均匀随机。
- `onSpinStart()` → 旋转 → `onSpinEnd()`：结果区先置「旋转中」，停下才展示，避免提前泄题。

**小程序适配**
- 分支模式省内存（扇区少）；题目模式扇区多（可达 50+），注意小程序 canvas 性能，必要时限制 `qSampleN`（如 ≤ 48）。
- `drawnIds`、抽样结果都应在页面 `data` 中维护，模式切换/设置变更时调 `rebuild()` 重建。

---

## 3. ② 内容开关 + 高能模式联动（最容易漏）

**设计意图**：四个类总开关 + 一个「高能模式」总闸，开启后自动解锁成人内容。

**交互**
- 四类 checkbox → `settings.enabled[cat]`。
- `adultMode` 开关有**联动副作用**（不是单纯一个布尔）：

```js
adultMode change 时：
  若开启：
    QB.topics.truth/deeptalk 中 adultOnly=true 的话题 → 自动勾上
    若 settings.maxContact < 3 → 设 maxContact=3（放开大冒险「全场尖叫」3级）
  若关闭：
    上述 adultOnly 话题 → 自动取消
    若 settings.maxContact > 2 → 设回 2
  然后 renderTopicChecks(); rebuild();
```

**过滤逻辑（所有题进转盘前的统一闸门 `isActive(q)`）**
```js
if (!settings.enabled[q.cat]) return false;
if (q.enabled === false) return false;
if ((q.adultOnly || q.risk === 'spicy') && !settings.adultMode) return false;  // 高能门
// 真心话/Deeptalk：话题范围开关
if (truth/deeptalk) if (settings[cat+'Topics'][q.topic] === false) return false;
// 大冒险：尺度/陌生人
if (dare) { if (q.contact > settings.maxContact) return false; if (q.stranger && !settings.allowStranger) return false; }
// 直接喝：量级/指定他人
if (drink) { if (AMOUNT_LEVEL[q.amount] > AMOUNT_LEVEL[settings.maxAmount]) return false; if (q.who!=='self' && !settings.allowOthers) return false; }
```

> **小程序重点**：`adultMode` 的联动必须照搬，否则用户开高能模式却看不到成人题、或大冒险 3 级仍被卡住。建议把这段联动封装成一个 `applyAdultMode(on)` 函数。

---

## 4. ③ 真心话 / Deeptalk · 话题范围

**设计意图**：在「真心话/Deeptalk 放进转盘」的前提下，再精细勾选哪些话题范围生效。

**数据**：话题来自 `QB.topics[cat]`（id + label + defaultOn + adultOnly）；开关状态存 `settings.truthTopics/deeptalkTopics`。

**实现**
- `renderTopicChecks()`：遍历 `QB.topics.truth` 渲染 checkbox（`data-topic="truth:<id>"`），再附一段「Deeptalk 话题：」分隔，遍历 `deeptalk`。
- 变更：`settings[cat+'Topics'][id] = checked; rebuild();`
- 高能模式开启时，带 `adultOnly` 的话题随 `adultMode` 自动勾选/取消（见 §3）。

**小程序适配**：话题列表可能较长，小程序里用 `scroll-view` 包住；`data-topic` 改成 `data-cat`/`data-id` 两个属性或 `bindtap` 传参。

---

## 5. ④ 大冒险 · 尺度

**设计意图**：控制大冒险的激烈程度上限 + 是否允许骚扰陌生人。

**配置项**
- `allowStranger`（默认 true）：未勾则 `stranger` 题不进转盘。
- `maxContact`（默认 2，可 0~3）：大于该值的 `contact` 题不进转盘。

**联动**：`maxContact` 受高能模式影响（开启时强制 ≥3，关闭时强制 ≤2，见 §3）。

**显示**：`branchLabel()` 对大冒险返回 `['社牛solo','礼貌试探','升温互动','全场尖叫'][q.contact]`，`stranger` 题显示「陌生人互动」。

**小程序适配**：`maxContact` 用 `picker` 或分段控件（4 档）；`allowStranger` 用 `switch`。

---

## 6. ⑤ 直接喝 · 量级（含「指定谁喝」）

**设计意图**：控制饮酒量级上限 + 是否允许指定他人喝/接力。

**配置项**
- `allowOthers`（默认 false）：未勾则 `who !== 'self'` 的题不进转盘。
- `maxAmount`（默认 `'glass'`）：强度映射
  ```js
  const AMOUNT_LEVEL = { sip:0, 'half-glass':0.5, glass:1, chug:2, bottle:3 };
  // 过滤：AMOUNT_LEVEL[q.amount] > AMOUNT_LEVEL[settings.maxAmount] 的不进转盘
  ```

**量级三档（新增题目时给的下拉）**
| value | 显示 |
|---|---|
| `sip` | 最温和 · 趣味喝法 |
| `glass` | 喝一杯 |
| `chug` | 罚酒干杯（最猛） |

**「指定谁喝」两类**（用户明确要求简化）
| value | 显示 |
|---|---|
| `self` | 自己喝 |
| `other` | 指定其他人喝/接力 |

**显示**：`branchLabel()` 对直接喝返回：含附加题→「喝+一道题」；`who!=='self'`→「指定他人/接力」；否则 `{sip:'趣味喝法', glass:'喝一杯', chug:'罚酒干杯', …}[amount]`。

**新增题目面板的二级联动**（小程序必做）
- 选 **直接喝**：显示「量级」下拉（3 档）+「指定谁喝」下拉（self/other）。
- 选 **大冒险**：显示「尺度」下拉（contact 0~3），**不显示**指定谁喝。
- 选 **真心话/Deeptalk**：显示「话题范围」下拉 + 「＋新增分类」按钮，**不显示**量级/谁。
- 选其他（无二级）：全部隐藏。

> 实现函数：`updateEdNewOptions()`（新增面板联动）、`levelOptionsFor(cat)`（提供 dare/drink 二级选项）、`topicOptionsFor(cat)`（提供 truth/deeptalk 话题）。

---

## 7. ⑥ 题库编辑 / 导入导出

### 7.1 筛选 / 搜索
- `edCat`（主分类，含「全部分类」）+ `edRefine`（二级，按主分类动态联动，见下）+ `edSearch`（关键字）。
- `updateEdRefineFilter()`：主分类为 truth/deeptalk → 标签「话题范围」+ 话题选项；为 dare → 「尺度」+ 0~3；为 drink → 「量级」+ 3 档；其他 → 隐藏。
- 过滤：`dare` 按 `q.contact` 匹配，`drink` 按 `q.amount` 匹配，其他按 `q.topic` 匹配；再叠加关键字 `includes`。

### 7.2 新增题目（三级联动）
- 输入题干 → 选主分类 → 按 §6 规则出现二级控件 → 点「＋ 新增」。
- 新题 `id`：`cat + '-user-' + Date.now()`。
- 写入字段随分类不同（见 §1.3）；默认 `risk:'safe'`、`difficulty:1`、`enabled:true`。
- 新增后 `renderEditor(); rebuild()`，即时生效，无需刷新。

### 7.3 新增二级分类（仅真心话/Deeptalk）
- `addNewTopic()`：读 `edNewCatName` → 去重（同名不改）→ 写 `QB.topics[cat].push({id: cat+'-u-'+Date.now(), label: name})` → 同步下拉并选中。
- 大冒险/直接喝的二级是固定档位，不提供新增。

### 7.4 编辑器表格
列：用(启用开关) | 题干(可改文本) | risk(safe/mild/spicy 下拉；选 spicy 自动置 `adultOnly=true`) | 尺度(dare=contact 0~3；drink=amount 含 legacy 兜底) | 删。
- 改任何一项 → `rebuild()`（转盘实时重建）。
- 「用」取消勾选 → `q.enabled=false`（该题移出转盘，但数据保留，可再勾回）。
- 删除 → 从 `QB.questions` 移除。

### 7.5 导入 / 导出
- **导出 JSON**：`Blob([JSON.stringify(QB)])` 下载，无损（含 topics 注册表）。
- **导入 JSON**：`FileReader` 读文件 → 校验 `questions` 数组 → 整体覆盖 `QB`（`ensureTopics()` 补齐 topics）→ 刷新所有面板 → `afterImportReset()`（清理失效暗箱引用、重置轮次）。
- **导出 CSV**：超集字段（`CSV_FIELDS`），带表头，方便 Excel 批量改文字。
- **导入 CSV**：解析后重建 `QB`（topics 回退为 id，因 CSV 不存话题标签）。
- **自定义题库指南**：内联 `window.GUIDE_MD`（build 时注入），点按钮直接下载 `.md`，不跳仓库。

**小程序适配（重点，Web API 在小程序不可用）**
- 导出 JSON：用 `wx.getFileSystemManager().writeFile` 写临时文件 → `wx.saveFileToDisk`（用户保存）或 `wx.shareFileMessage` 分享。
- 导入：用 `wx.chooseMessageFile({count:1, type:'file'})` 选文件 → `FileSystemManager.readFile({encoding:'utf-8'})` 拿字符串 → JSON.parse。
- 指南 `.md`：小程序无 Blob 下载，改为 `wx.setClipboardData({data: GUIDE_MD})` 让用户复制全文，再去粘贴给 AI。
- CSV 解析：小程序无 DOM，用纯 JS 的 `parseCsv()`（网页版已实现，可直接复用逻辑）。

---

## 8. ⑦ 暗箱操作（指定某次必中）

**设计意图**：开局前预设「第 N 次 / 前 K 次」必中某类别或某具体题，用于剧本式玩法。

**规则类型（4 种）**
| type | 含义 | 关键字段 |
|---|---|---|
| `exact` | 第 N 次必中某类别 | `spin:N, cat` |
| `prefix` | 前 K 次必中某类别 | `count:K, cat` |
| `exactQ` | 第 N 次必中某题 | `spin:N, qid` |
| `prefixQ` | 前 K 次必中某题 | `count:K, qid` |

**命中优先级 `resolveRigging(n)`（n=当前轮次）**
1. 先匹配 `prefixQ`/`prefix`（`n <= count` 即命中）；
2. 再匹配 `exactQ`/`exact`（`spin === n` 且 `usedExact` 未消费过，一次性）；
3. 都无 → 返回 `null`，走正常随机。

**保护机制 `isRiggingProtected(id)`**：被 `exactQ`/`prefixQ` 引用的题，在不重复模式下也**不从转盘移除**，保证能反复命中。

**按题指定流程**：`rigSearch` 搜当前生效题（最多 20 条）→ 点选 `rigSelQid` → 填轮次 → 添加 `exactQ`/`prefixQ` 规则。

**小程序适配**
- 规则数组 `rigging.rules` 存本地（`wx.setStorageSync`）。
- `spinCount` 重置时 `rigging.usedExact.clear()`。
- `prefix` 类在「前 K 次」范围内每次生效；`exact` 类一次性，建议 UI 上标「已用/未用」状态。

---

## 9. ↻ 立即重建

```js
function rebuild() {
  buildBranches();        // 分支模式数据
  buildQuestionSegs();    // 题目模式抽样
  renderWheel();          // 刷新转盘扇区（已存在则改 prizes，否则 new LuckyWheel）
  updateBankStat();       // 更新「题库 N 题」统计
}
```
任何设置变更后都调它。小程序里把 `rebuild()` 作为统一刷新入口，避免散落的状态不一致。

---

## 10. 小程序通用适配清单（落到代码前的检查项）

1. **状态管理**：`settings` + 用户自定义后的 `QB` 用本地缓存持久化；`CAT_META`、二级选项等写死在代码。
2. **字体**：网页用 Google Fonts `ZCOOL KuaiLe` + `Nunito`；小程序不可用外链，需 `@font-face` 本地化（参考《小程序-字体与字号规范.md》）。
3. **单位**：设计稿 px × 2 = rpx。
4. **转盘**：网页用 `lucky-canvas@1.7.25`（CDN）；小程序改用 npm 包 `@lucky-canvas/wx`，API 类似（`new LuckyCanvas.LuckyWheel(...)`）。
5. **抽屉/弹层**：`settingsDrawer` 是右侧滑出（`transform: translateX` + `backdrop`），小程序用自定义组件 + 半屏弹层；注意 `cover-view` 层级（转盘 canvas 上的浮层需 cover-view）。
6. **导入导出**：见 §7.5，用 `wx.chooseMessageFile` / `FileSystemManager` / `wx.setClipboardData` 替代 `FileReader`/`Blob`。
7. **高能模式联动**：务必实现 §3 的 `applyAdultMode`，否则成人内容解锁不完整。
8. **二级联动**：实现 §6 的 `updateEdNewOptions` / `levelOptionsFor` / `topicOptionsFor`，保证新增题目时「用不着的二级框不显示」。
9. **重建入口**：所有设置变更统一走 `rebuild()`。

---

## 附：本次网页版已落地的关键修正（供小程序对齐）
- 「直接喝」二级叫**【量级】**（3 档：sip 趣味喝法 / glass 喝一杯 / chug 罚酒干杯），不是"尺度"。
- 「指定谁喝」简化为两类：`self`(自己喝) / `other`(指定其他人喝/接力)。
- 大冒险叫【尺度】(0~3)，真心话/Deeptalk 叫【话题范围】，三者二级框互斥显示。
- 高能模式 = 解锁 spicy 话题 + 放开大冒险 3 级。
